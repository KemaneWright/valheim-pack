## Settings file was created by plugin ShaderHelperForMac v3.4.1
## Plugin GUID: aaa.drummercraig.shaderhelperformac

[Diagnostics]

## Diagnostic-only. Comma-separated list of material base names (case-insensitive, after StripInstance normalization). When a material's name is in this list, BridgeMaterialProperties emits one [BridgeTrace] log line per bridge call with the source _EmissionColor, IsHdrEmission result, _EMISSION keyword state, and HasEmissiveIntent result. Empty disables the trace (default). Used to gather evidence for the v3.0.7 emission-bridge timing investigation; see LESSONS_LEARNED.md and CHANGELOG v3.0.7. Pure read-only observation — does not affect any gate, sweep, or resolver decision.
# Setting type: String
# Default value: 
Bridge Trace Mat Names = 

## Diagnostic-only. When true, SweepMaterialsCoroutine emits one [SweepProgress] log line per SweepBatchSize (=100) renderer batch, reporting per-batch wall-clock, cumulative sweep time, renderer index, mats processed, and replacement count. Used to identify where periodic-sweep wall-clock cost is concentrated when investigating frame-time stutter. Default false; enable for one diagnostic session, then disable. Pure read-only observation — does not affect any gate, sweep decision, or resolver outcome. See CHANGELOG v3.0.16.
# Setting type: Boolean
# Default value: false
Sweep Progress Trace = false

## Diagnostic-only. When a SINGLE renderer's sweep work exceeds this many milliseconds, the sweep emits one [SweepStall] line naming that renderer, its transform path, its material and shader names, how much of the time was spent resolving shaders by name, and the GC-collection counts across it. Set to 0 to disable. Default 500 and ENABLED: at this threshold it can only fire on an already-catastrophic frame, so normal sessions log nothing, and the stall it hunts is a one-shot load-time event that an opt-in flag would miss. Pure read-only observation — does not affect any gate, sweep decision, or resolver outcome. See CHANGELOG v3.2.6 and docs/STUTTER_INVESTIGATION_2026-08-24.md.
# Setting type: Single
# Default value: 500
Sweep Stall Threshold Ms = 500

## When true (DEFAULT), TryReplaceMaterial rebinds any material to the canonical Unity instance of its vanilla-named shader, ONCE per material per session, AND ONLY when the material is on a ParticleSystemRenderer this sweep. Fixes the dropped-item particle indicator (item_particle on Magic_Supremacy drops) without re-introducing the v2.1.27-class weapon flicker that v3.0.27's unscoped rebind caused. Set to false to disable the rebind entirely (restoring v3.0.28 behaviour: item_particle renders pink on dropped items, but no weapon flicker risk). The flag is provided as a kill-switch only — flipping to false is intended for users who observe regressions on materials this rebind class shouldn't have touched. See CHANGELOG v3.0.30 for the v3.0.29 evidence and v3.0.30 design.
# Setting type: Boolean
# Default value: true
Vanilla Rebind Enabled = true

## Default ON (v3.1.33). When true, a gate-admitted mod material whose current shader is already a supported additive / alpha-blended particle shader (e.g. 'Legacy Shaders/Particles/Additive', 'Particles/Standard Unlit') AND has no hard mat:/suffix: rule is LEFT ON ITS OWN SHADER instead of being routed through the aggressive-family Fallback bind to Custom/Creature (which drops the additive _TintColor/_InvFade controls — the flame_TW 'good look' loss). This reproduces, without a rule, what '[useshader] Legacy Shaders/Particles/Additive' achieves. Any user [forcefallback]/[forcetransparent]/[useshader] rule still wins (the branch is gated on no-hard-rule). Vanilla materials are unaffected (the provenance veto runs first). The v3.1.32 default-OFF opt-in was validated in-game (2165 [PreserveAdditive] fires across 36 distinct VFX materials on 4 shader families, zero solid geometry caught) and flipped to default-ON in v3.1.33. Set to false to disable the preservation branch and route additive particles through the aggressive-family Fallback. See CHANGELOG v3.1.32 + v3.1.33.
# Setting type: Boolean
# Default value: true
Preserve Additive Particles = true

## Default ON (v3.1.54, validated in-game). When true, a gate-admitted mod material whose current shader is in the 'HunFX/' family (the HunFX VFX shader pack — bursts / fire / embers / slashes, shader names 'HunFX/SH_HunFX_common', 'HunFX/SH_HunFX_simple', etc.) is automatically bridged to 'Legacy Shaders/Particles/Additive' — the same target users hand-write '[useshader]' rules for on these materials, applied automatically. This branch runs BEFORE the [useshader] consumption block, so it decides purely from the material's shader identity and ignores any [useshader] rule for HunFX materials by construction. Vanilla materials are unaffected (the provenance veto runs first). The v3.1.53 default-OFF opt-in was validated in-game (449 [HunFXAutoDefault] fires across 51 distinct VFX materials on 3 HunFX shaders, all landing Additive, zero solid geometry caught) and flipped to default-ON in v3.1.54. Set to false to disable the auto-default and route HunFX materials through the aggressive-family Fallback (or your own [useshader] rules). See CHANGELOG v3.1.53 + v3.1.54.
# Setting type: Boolean
# Default value: true
HunFX Auto Default = true

## Default ON (v3.2.2, validated in-game). When true, a gate-admitted mod material whose current shader is exactly 'Custom/Piece' AND whose name is NOT in the vanilla baseline AND that has no hard mat:/suffix: rule is automatically bridged to 'Custom/Creature' — the same target users hand-write '[useshader] mat:<name> "Custom/Creature"' rules for on mod mesh weapons / armor / props (e.g. AscendedArsenal's Daveblade set, HipLantern_mat, ClawMaterialBaseBlue). Such materials ship authored on a bundle copy of Custom/Piece that the plugin's canonical-ID skip leaves on action=Original, where they flicker / show offset pieces on Metal; Custom/Creature is the proven fix. Any user [forcefallback]/[forcetransparent]/[useshader] hard rule still wins (the branch is gated on no-hard-rule). Vanilla pieces are unaffected (the provenance veto runs first AND the branch additionally requires the material name be absent from the vanilla baseline). The v3.2.1 default-OFF opt-in was validated in-game (vanilla-leaks=0 across 10987 live materials via `shaderhelper checkredesign`, and the first toggle+sweep fixed the flicker and mis-alignment on the affected mod mesh materials) and flipped to default-ON in v3.2.2. Set to false to disable the auto-default and route Custom/Piece mod materials through their original binding (or your own [useshader] rules). See CHANGELOG v3.2.1 + v3.2.2.
# Setting type: Boolean
# Default value: true
Piece Mesh Auto Default = true

## Target Legacy particle shader for mod ParticleSystemRenderer materials that the plugin observes on the canonical 'Custom/Creature' shader (the GnistaGold case — a mod VFX material the canonical-ID skip otherwise leaves on action=Original, where it renders wrong on Metal). The user's empirical rule: 'no particle system renderer from a mod ever works on Custom/Creature.' This box automates the ~30 hand-written [useshader] particle rules across the per-mod txts. VALUES: leave EMPTY to disable the auto-default entirely (such materials are left exactly as-is); set to 'Legacy Shaders/Particles/Alpha Blended' (the default — softer blend, safe for smoke / fog / dust / sparks) or 'Legacy Shaders/Particles/Additive' (brighter, best for glows / embers / magic) to bridge every such material to that shader. Any other non-empty value is treated as a typo and ignored (the branch logs one warning and stays inert). Materials with their own [useshader]/[forcefallback]/[forcetransparent] hard rule are never touched (the rule wins). Vanilla materials are never touched (the provenance veto + the not-in-baseline guard run first). See CHANGELOG v3.2.3.
# Setting type: String
# Default value: Legacy Shaders/Particles/Alpha Blended
Particle Creature Auto Default Target = Legacy Shaders/Particles/Alpha Blended

## Default ON. When true, a material whose runtime shader is Unity's 'Hidden/InternalErrorShader' placeholder (the magenta error shader Unity substitutes when a real shader fails to compile or load on Metal) is admitted to the sweep and bound to the Fallback shader (Custom/Creature) so it renders visibly instead of staying pink. Set to false to leave such materials magenta — the opt-in 'show me the pink items that were on this shader' diagnostic: both recognition sites (MaterialClassifier.NeedsReplacement and ActionResolver.ResolveByShaderName) fall through, and because 'Hidden/' is on the safe-shader prefix list NeedsReplacement returns false, leaving the material on its error-shader binding. This is a sweep-side filter toggle, NOT a provenance-gate change — vanilla materials rendering correctly are never affected (their shader is not Hidden/InternalErrorShader). See CHANGELOG v3.1.34.
# Setting type: Boolean
# Default value: true
Repair Internal Error Shader = true

## Diagnostic-only (default OFF). When true, a read-only Harmony postfix on Valheim's MeleeWeaponTrail.Start emits one [TrailProbe] log line per weapon-trail component as it is created, reporting the trail material's name + shader + the owning GameObject path. The vanilla weapon swing/sweep trail is a runtime-generated mesh on a detached GameObject('Trail') with a MeshRenderer — NOT a TrailRenderer, NOT a particle — so it cannot be named by `nearby` / `capture` / `watchr` or by the offline bundle tools. Enable this, swing a weapon, then read the [TrailProbe] lines in LogOutput.log to get the material name. Pure read-only observation — mutates no material, renderer, gate, sweep, resolver, or cache. Disable when done. See CHANGELOG v3.1.56.
# Setting type: Boolean
# Default value: false
Trail Probe = false

## Diagnostic-only (default OFF). When true, the [BindProbe-*] shader-collision diagnostics in the bundle-load / Awake paths (OnLoadAsset, MaterialLoad, GameObjectMaterial, ShaderLoad, Baseline, RetroactiveSweep) emit [BindProbe-*] LogInfo lines and run their Resources.FindObjectsOfTypeAll<Shader>() peer-count scans. These are pure read-only observation used to localize vanilla-shader-name collisions during development; they mutate no material, gate, sweep, resolver, baseline, or cache state. Left OFF, the plugin skips the scan + log work entirely on every bundle load and Awake snapshot — recommended for normal play. Enable only for one diagnostic session when asked to capture [BindProbe-*] evidence, then disable. See CHANGELOG v3.1.65.
# Setting type: Boolean
# Default value: false
Diagnostic Probes = false

## Default OFF (opt-in). When true, a gate-admitted mod material whose source _Metallic >= 0.5 AND that has a metallic-gloss map bound (_MetallicGlossMap / _MetallicMap) has its smoothness slots written (_UseGlossmap=1, _MetalGloss=0.7, _Glossiness=0.9, _Smoothness=0.9) after the Fallback bridge so the matte-metal blade (e.g. BronzeClaymore_mat_TW: _Metallic=1 + metallic-roughness map + smoothness 0 = dark diffuse metal on Metal) reads polished instead of dark. This automates, without a per-sword rule, the fix the user validated by hand via '[useshader] mat:<Blade> Custom/Creature _UseGlossmap=1 _MetalGloss=0.7 _Glossiness=0.9'. The write lands at the POST-BRIDGE Frame-B site the v3.1.49 probe proved has the writable slots (the reverted v3.1.45–47 wrote inside the bridge against a different, slot-less Custom/Creature instance and no-op'd three times). Non-metals (antler/leather/wood, all _Metallic=0) never enter the branch. Any per-mod [useshader] rule still wins (a ruled material returns before reaching this branch). Vanilla materials are unaffected (the provenance veto runs first). Sweep-side property write, NOT a provenance-gate change. Set to true to enable; validate that the intended blades polish and no non-metal changes, then a future release may flip the default ON. See CHANGELOG v3.1.49 + v3.1.50.
# Setting type: Boolean
# Default value: false
Auto Polish Metallic Materials = false

## Default ON (v3.1.52, validated in-game). When true, an equipped-item material the plugin has already bridged to Custom/Creature that carries an ASSIGNED metallic-gloss texture (_MetallicGlossMap has a real texture, not just the empty property slot) has its smoothness slots written so mod-metal weapons (e.g. Warfare IronSledge_mat_TW / bronzedark_mat_TW) read polished without a per-weapon [useshader] rule. The assigned-texture gate cleanly separates metal from equipped wood/leather (whose _MetallicGlossMap is null), which the v3.1.50 'Auto Polish Metallic Materials' _Metallic>=0.5 gate could not (mod authors ship metal weapons at _Metallic 0–0.2). The written values are the tunable 'Auto Polish Use Glossmap' / 'Auto Polish MetalGloss' / 'Auto Polish Glossiness' entries below. Does NOT raise _Metallic and does NOT touch emission (a glowing mod weapon needs a per-mod [useshader] _EmissionColor rule). Only materials already on Custom/Creature (gate-admitted, mod-owned, plugin-bridged) are touched — a correctly-rendering vanilla item is never affected. Sweep-side property write at the equip hook, NOT a provenance-gate change. See CHANGELOG v3.1.51.
# Setting type: Boolean
# Default value: true
Auto Polish Equipped Metal = true

## Tuning for 'Auto Polish Equipped Metal'. The _Metallic value written to polished equipped-metal materials (0 = dielectric/plastic, 1 = fully metallic). Default 1.0. Mods routinely ship metal weapons at _Metallic 0–0.2 (the validated iron-knife knifeironblade_mat_TW = 0), which on a PBR lit shader renders as a glossy dielectric rather than metal even after the gloss writes land — so raising metalness is needed in addition to the smoothness values. Edit, then re-equip the weapon (or reload + sweep) to see the change. Only has effect when 'Auto Polish Equipped Metal' is enabled. See CHANGELOG v3.1.52.
# Setting type: Single
# Default value: 1
Auto Polish Metallic = 1

## Tuning for 'Auto Polish Equipped Metal'. When true (default), the auto-polish writes _UseGlossmap=1 so the bound metallic-gloss map's alpha drives per-texel smoothness; false writes _UseGlossmap=0 (uniform smoothness from the MetalGloss/Glossiness values). Only has effect when 'Auto Polish Equipped Metal' is enabled. See CHANGELOG v3.1.51.
# Setting type: Boolean
# Default value: true
Auto Polish Use Glossmap = true

## Tuning for 'Auto Polish Equipped Metal'. The _MetalGloss value written to polished equipped-metal materials (0 = matte, 1 = mirror). Default 0.7. Edit, then re-equip the weapon (or reload + sweep) to see the change. Only has effect when 'Auto Polish Equipped Metal' is enabled. See CHANGELOG v3.1.51.
# Setting type: Single
# Default value: 0.7
Auto Polish MetalGloss = 0.7

## Tuning for 'Auto Polish Equipped Metal'. The _Glossiness value written to polished equipped-metal materials (0 = matte, 1 = mirror). Default 0.9. Edit, then re-equip the weapon (or reload + sweep) to see the change. Only has effect when 'Auto Polish Equipped Metal' is enabled. See CHANGELOG v3.1.51.
# Setting type: Single
# Default value: 0.9
Auto Polish Glossiness = 0.9

[Fixes]

## Default ON. Jotunn's embedded 'minimapmanager' asset bundle ships its MinimapCompose shaders with D3D11 and Vulkan subprograms only — there is no Metal variant — so on macOS the overlay compose blit inside MinimapManager.DrawLayer fills the whole 2048x2048 overlay texture with the error colour, and because that texture is drawn full-rect over the large map AND the minimap, the entire world map renders magenta whenever any Jotunn MapOverlay is enabled (e.g. StarLevelSystem's distance rings and zone overlay). When true, that compose step is performed on the CPU instead, using no shader at all: each overlay is alpha-composited into a plugin-owned buffer with the same lerp(dst, src, src.a) Jotunn's shader used, and that buffer is uploaded to the overlay texture ONCE per redraw with no GPU readback anywhere in the path, so multi-overlay accumulation is preserved. Nothing is patched at all until a mod actually registers a Jotunn overlay. A stock-shader GPU substitute was tried first and abandoned — the only suitable built-in (Unlit/Transparent) is stripped from Valheim's build, and every other candidate either premultiplies, masks off alpha, or needs a vertex colour that Graphics.Blit does not supply. TRADE-OFF: Jotunn's 'if (fog.r == 1) alpha = 0' clause is not reproduced (it would cost a full 2048x2048 GPU readback per redraw, the exact cost this path exists to avoid), so overlay pixels are no longer hidden in unexplored terrain, and because that clause zeroed the ACCUMULATED alpha rather than just the incoming overlay's, a later fogged overlay no longer erases earlier overlays in fogged regions. Set to false to restore Jotunn's original path, which on macOS means the magenta map returns. REQUIRES A RESTART either way: this setting is read once when the hooks are installed and is never re-read, so changing it mid-session has no effect. Only the overlay compose is substituted; MapDrawings (ComposeMain/Height/Forest/Fog) is untouched and remains broken on Metal. No vanilla Valheim asset is modified.
# Setting type: Boolean
# Default value: true
Fix Jotunn Minimap Overlay = true

[General]

## How often (in seconds) to re-sweep all materials. The periodic sweep is now a safety net for unobserved bundle-load / prefab-spawn events; the primary mechanisms are Harmony postfixes on ZNetScene.Awake, Location.Awake, AssetBundle.Load*, VisEquipment.AttachItem, and the scene-load handler. Default raised to 600 s in v3.0.63 because the v2.1.17 admission-epoch skip-gate already short-circuits every periodic sweep on a stable scene; the cadence only matters when the safety cap (10 consecutive skips) forces a full sweep. Lower this (e.g. 120 or 30) if you run VFX-heavy mods that spawn materials frequently and you observe pink materials between sweeps. Set to 0 to disable the periodic timer entirely.
# Setting type: Single
# Default value: 600
Periodic Sweep Interval = 600

